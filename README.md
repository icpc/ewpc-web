# EWPC Website

Website for the European Women's Programming Contest (EWPC), served by GitHub Pages.

Plain static HTML — no build step. `index.html` is the whole site; `.nojekyll` disables Jekyll processing.

To publish: GitHub repo → Settings → Pages → deploy from branch `master`, folder `/ (root)`.
